package com.elams.enums;

/**
 * Enumeration representing the roles of employees.
 * This enum defines the possible roles that an employee can have within the system.
 */
public enum EmployeeRole {
    /**
     * Represents a manager role.
     */
    MANAGER,

    /**
     * Represents a regular employee role.
     */
    EMPLOYEE
}